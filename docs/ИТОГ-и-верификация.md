# Итог сессии 03–04.07.2026 + верификация каждого пункта

**Прод = локаль = `01c2598`** (bilimos.kg = erudit/main = локальный HEAD, синхронно). Веном-стек: Fable (план) → Opus (ревью+приёмка) → Codex (код). Каждый пункт ниже — с колонкой **«проверено чем»** (не на словах: живые HTTP-коды прода, e2e, инспекция кода, локальные прогоны).

Верификация прогнана 04.07 против прода:
`/ /calculator /login /api/health` → **200**; калькулятор рендерит модель Эмира; лендинг-CTA на месте; защищённые API без auth → **401**; CI последний прогон → **success**; secretary1 (branch-скоуп) → **165 учеников** (fail-closed не запер).

---

## ✅ Сделано и ПРОВЕРЕНО (в проде)

| Коммит | Что | Проверено чем |
|--------|-----|---------------|
| `0e3aa69` | RBAC-Ф1 — каждая роль видит своё | e2e 21/21 |
| `72131ed` | On-prem Docker-пакет | `docker build --check` (реальный сервер ждёт аренду) |
| `c0f193d` | Калькулятор → заявки (TariffLead) | таблица создана db push |
| `cbf9fe2` | fix .dockerignore denylist (nixpacks) | билд Coolify встал |
| `50ee4d6` | Публичный /calculator без логина | 200 аноним на проде |
| `e37a768` | fix: коммит ребренд тема/лого | build прошёл |
| `768d78c` | Калькулятор — модель Эмира v1.1 | маркеры «Медиум/Размер/Свой набор» на проде; числа 1050000/240000 сверены |
| `31d88a6` | RBAC-Ф2 — приватность ассистента | e2e 14; cloud-инвариант mock-fetch (ФИО→[ЛИЦО_N]) |
| `47a1d3b` | RBAC-Ф3 — изоляция филиалов | e2e 21; leakedSouth 0 |
| `af80314` | Лендинг: CTA «Рассчитать стоимость» | «Рассчитать стоимость» в HTML прода |
| `c54326d` | FR-12 rate-limit публичных | curl-цикл: 1-10→200, 11-й→**429** на проде |
| `b46e15f` | G7 каркас печати трудового договора | 2× TODO(G7); печать полей; tsc |
| `ae5bea5` | Бухгалтер фильтр по классам + branch-fix | finance/journal 401; single-branch числа не изменились |
| `9a83b7d` | Ребренд ERUDIT→Bilim OS завершён | 0 eruditBlue в дереве; home 200 |
| `a01950e` | CI-гейт (types+lint+build) | GitHub Actions прогон **success** |
| `10b8a91` | Завуч — «Пробелы в журнале» /analytics | API 401 за ролями; /analytics 200 |
| `74284a4` | RBAC-Ф3.1 — fail-closed + утечки grades/attendance/homework | e2e 21; secretary1 видит 165 учеников (не заперт); grading 200 |
| `d33fc68` `f0e79ff` | CI e2e-rbac job (+ретраи) | сиды на чистой БД (11 аккаунтов ✓); прогон **check+e2e-rbac → success** |
| `01c2598` | Финансы: переплата + досрочное погашение | локальный прогон а-г с числами; payoff 401/403; penalty/carry/renew НЕ в диффе; **CI-прогон success** (check+e2e-rbac) |

## Автономные батчи (веном)
**Батч 1:** FR-12 · G7 · бухгалтер-фильтр+branch-fix · «Пробелы в журнале» · ребренд-cleanup · CI-гейт — все ✅ в проде.
**Батч 2:** RBAC Ф3.1 · CI e2e-job · финансы (переплата/досрочное) — все ✅ в проде.

### Финансы — усиленная приёмка (эмпирика Codex на :5433)
- **а** оплата=остатку (10000) → ровно **1 Payment**, paid (регрессии нет).
- **б** переплата 15000 → 10000 на месяц2 + 5000 на месяц3; remaining 20000→5000; StudentNote.
- **в** досрочное → долг 0, **contract.status active→active** (не менян).
- **г** call_center переплата → **400**, 0 строк.
- penalty.ts / carry / renew-contract / invoice-status — **0 байт правок** (git diff).

### Обоснованно НЕ сделано (реализация = регрессия)
- **Ф2-домаскировка lesson-plan/presentation** — эвристика маскировала бы «Пушкина/Дарвина» в темах уроков → сломала бы генерацию. Ассистентская приватность (главный вектор) уже закрыта в Ф2.
- **Cambridge/физрук выделенные экраны** — покрыто наследованием / ролью teacher (Fable-аудит).

---

## ⏸ Отложено — гейт (не могли без Эмира/сервера/доступа)
| Пункт | Гейт |
|-------|------|
| College Consulting (роль+продукт) | ТЗ Эмира (нет данных-модели) |
| Полный текст трудового договора | шаблон Эмира (каркас G7 готов, TODO в 2 константах) |
| Индексация /calculator (noindex→open) | утверждение прайса v1.1 Эмиром |
| #2 миграции вместо db push | доступ к прод-Neon + осторожный baseline |
| Сервер + hardening | аренда сервера Intellect |
| AI/токен-модель родителя | не финализирована у Эмира |

## 🔧 Follow-up (техдолг, не блокирует, безопасно отдельно)
- **~40 lint-ошибок в src** (`react-hooks/set-state-in-effect`) — lint в CI non-blocking; чинить аккуратно покомпонентно → затем убрать `continue-on-error`.
- Онбординг по ролям (Ф5) — объёмно.
- Финансы: видимый credit-«кошелёк» (сейчас авто-предоплата); сбор пени деньгами (сейчас advisory) — только по решению Эмира.
- secretary в canPay UI но не в payments API (предсущее рассогласование) — тривиальный фикс отдельно.
- RBAC: schedule по явному чужому classId (не список) — следующая итерация.

---
_Каждый ✅ подтверждён живой проверкой прода/e2e/кода. Детали ценовой модели — `project_calculator_emir_pricing`, RBAC — `project_rbac_phases_done`, деплой — `project_deploy_nixpacks_rebrand`._
