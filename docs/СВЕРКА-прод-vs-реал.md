# Сверка «прод vs реал» — сессия 03–04.07.2026

**Итог: прод = локаль = `10b8a91`.** `bilimos.kg` (remote `erudit/main`) синхронен с локальным HEAD — весь код закоммичен и задеплоен. Ниже — что именно, для быстрой сверки.

Деплой-флоу сессии: `git push erudit main` → **ручной триггер Coolify API** (авто-вебхук НЕ поднимает сборку!) → мониторинг top-level `status` деплоя (`finished`/`failed`) → проверка `bilimos.kg` вживую. Coolify собирает через **nixpacks** (не наш Dockerfile). См. память `project_deploy_nixpacks_rebrand`.

---

## ✅ В ПРОДЕ — 16 коммитов сессии (снизу вверх)

| Коммит | Что | Проверено на проде |
|--------|-----|--------------------|
| `0e3aa69` | **RBAC-Ф1** — каждая роль видит своё (10 утечек/11 роутов) | e2e 21/21 |
| `72131ed` | On-prem Docker-пакет (Dockerfile+Caddy+compose.prod+env) | ждёт реальный сервер |
| `c0f193d` | Калькулятор → заявки (модель TariffLead) | — |
| `cbf9fe2` | fix: `.dockerignore` denylist (чинит билд nixpacks) | билд встал |
| `50ee4d6` | Публичный `/calculator` (без логина) | 200 аноним |
| `e37a768` | fix: коммит ребренд тема/лого (чинит build BilimosLogo) | билд встал |
| `768d78c` | **Калькулятор — модель Эмира v1.1** (вес×размер школы) | маркеры на проде ✓ |
| `31d88a6` | **RBAC-Ф2** — приватность ассистента (ФИО не в облако) | e2e 14, cloud-инвариант mock-fetch |
| `47a1d3b` | **RBAC-Ф3** — изоляция филиалов (без миграции, через relation) | e2e 21, leakedSouth 0 |
| `af80314` | Лендинг: CTA «Рассчитать стоимость» → /calculator | CTA на проде ✓ |
| `c54326d` | **FR-12** — rate-limit публичных эндпоинтов | curl-цикл 11-й→429 |
| `b46e15f` | **G7** — каркас печати трудового договора (TODO шаблон Эмира) | finished |
| `ae5bea5` | **Бухгалтер** — фильтр по классам + закрытие branch-leak Ф3 | finished |
| `9a83b7d` | Ребренд ERUDIT→Bilim OS завершён + убран алиас eruditBlue | home 200 |
| `a01950e` | **CI-гейт** GitHub Actions (фаза 1: types+lint+build) | Actions запустился |
| `10b8a91` | **Завуч** — секция «Пробелы в журнале» на /analytics | API 401/страница 200 |

## Автономный прогон (веном: Fable план → Opus ревью → Codex код)
| # | Задача | Статус |
|---|--------|--------|
| 1 | FR-12 rate-limit | ✅ прод |
| 2 | G7 печать договора (каркас) | ✅ прод |
| 3 | Бухгалтер фильтр + branch-fix | ✅ прод |
| 4 | «Пробелы в журнале» (завуч) | ✅ прод |
| 5 | Ребренд-блоб cleanup | ✅ прод |
| 6 | CI-гейт (фаза 1) | ✅ прод |
| 7 | Гайды по ролям | ⏸ отложено (крупно/скриншоты) |

Оценкой Fable отсечено как НЕ нужное: Cambridge-экран (нет данных-сущностей, наследует завуча), Физрук-экран (= роль teacher, phys-norms+КТП уже есть).

---

## 🟡 Локально, НЕ критично (не код продукта)
- `package.json` — `name: erudit→bilimos` (косметика, закоммичена этим коммитом вместе со сверкой).
- `BUGS.md` — рабочие QA-заметки journey-tester (не код).
- `docs/*.md/.docx`, scratch-скрипты в корне (`jpsy_card*.mjs`, `lib_e9_test*.mjs`, `qa_*.mjs`) — черновики/тесты, не в продукте.

## ⏸ Отложено — гейт (не могли сейчас)
- **College Consulting** — новая роль + продукт (поступление/экзамены), нет данных-модели → ждёт ТЗ Эмира.
- **Финансы: переплата + досрочное погашение рассрочки** — реальный пробел, чувствит. логика → только с явным ок Алана.
- **#2 миграции вместо db push** — нужен доступ к прод-Neon + осторожный baseline (риск на живой БД).
- **Сервер + hardening** — ждёт аренду сервера Intellect (on-prem пакет готов).
- **Полный текст трудового договора** (G7) — шаблон Эмира; сейчас каркас с TODO в 2 константах.
- **Индексация /calculator** (noindex→open) — когда Эмир утвердит прайс v1.1.

## 🔧 Follow-up (техдолг, не блокирует)
- **CI e2e-rbac job (фаза 2)** — нужна локальная проверка сидов на чистой БД (иначе флап); сейчас гейт = только check.
- **~40 lint-ошибок в `src`** (в осн. `react-hooks/set-state-in-effect`) — lint в CI non-blocking; починить → убрать `continue-on-error`.
- **RBAC Ф3.1** — скоуп grades/attendance/schedule (сейчас там RBAC+classId уже не дают утечки списком); fail-closed для non-admin без branchId.
- **Ф2**: маскировка lesson-plan.ts/presentation.ts; fail-closed для behaviorIncident.
- **TELEGRAM_WEBHOOK_SECRET** на проде — проверить что задан (ops).

---
_Сгенерировано по факту git-лога и деплоев. Для деталей ценовой модели — `PLAN-остаток-MVP.md`, память `project_calculator_emir_pricing`, `project_rbac_phases_done`._
