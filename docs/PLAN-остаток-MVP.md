# План: остаток MVP — автономный прогон (на одобрение Алана)

Источники: смета `Bilim_OS_smeta_srokov_MVP.docx`, `TZ-codex-остаток-2.md` (G1-G7), `TZ-codex-остаток-Pravki.md` (P1-P6), модель Эмира.
Стек: **Fable** (план/спека) → **Opus** (ревью+приёмка) → **Codex** (код). Каждая задача: спека→ревью→код→приёмка→коммит→push+триггер Coolify→**сам проверяю деплой**.

## Контекст сверки (что УЖЕ в проде — не трогаем)
RBAC Ф1-Ф3 · калькулятор Эмира v1.1 + публичный /calculator + CTA на лендинге · on-prem Docker-пакет · G1-G6 (логин ученику, HR-онбординг G2/G3, акт инвентаря G4, 2-шаг согласование G5, часы учителя G6) · P1-P6 в проде (ивент+RoleFeedback, учредитель, медиа, telegram-QR, PhysNorm, очередь→воронка).

---

## ДЕЛАЮ (доступно без сервера/Эмира) — по порядку

### 1. FR-12 — rate-limit публичных эндпоинтов  `[Codex готов, жду одобрения на коммит]`
Публичный submit калькулятора открыт (honeypot есть, лимита нет = спам). Лимиты: tariff-leads 10/час/IP, applications 5/час, onboarding 10/час. Файлы: `rate-limit.ts` (+1 хелпер) + 3 публичных роута. Fail-open, 429+Retry-After. → деплой.

### 2. G7 — печать трудового договора (каркас)
`src/shared/lib/contract/print-staff-contract.ts` по образцу `print-contract.ts` (поля StaffContract: ФИО/должность/оклад/даты) + кнопка «Печать» в /hr. Текст-рыба с TODO «шаблон Эмира». → деплой.

### 3. Бухгалтер — фильтр по классам
На экране бухгалтера (платежи/долги) добавить фильтр по классу. Мелкий greenfield. → деплой.

### 4. Мелкие экраны-доработки
Заучи: дашборд наполняемости оценок (если нужен сверх журнала); Cambridge/физрук — выделенные экраны (роли есть через наследование). Сначала оценю нужность по коду, сделаю только реально полезные. → деплой.

### 5. Ребренд-блоб cleanup  `[техдолг]`
Закоммитить 18 непереведённых `page.tsx` (свап eruditBlue→bilimosBlue) + убрать алиас `eruditBlue` из темы. Проверка build (Linux-регистр). → деплой. Закрывает наполовину сделанный рибренд.

### 6. #3 — CI-гейт
`.github/workflows`: typecheck + lint + e2e (rbac-leaks) на PR. Dev-инфра, НЕ деплой прод. (Полноценные окружения beta/kappa/delta — позже, нужен Coolify-сетап.)

### 7. Гайды по ролям (dev-часть)  `[крупнее, в конце]`
ИИ-проход по ролям + скриншотилка → черновики статей. Объёмно/размыто — сделаю базовый каркас, полировка отдельно.

---

## ТРОГАЮ ТОЛЬКО С ТВОИМ ОК (чувствительное)

### Ф. Финансы — распределение переплаты + досрочное погашение рассрочки
Реальный пробел, но логика чувствительная (verified-only долг, пеня-кап, перенос). НЕ делаю автономно — если скажешь «делай финансы», сделаю с усиленным ревью.

---

## ОТЛОЖЕНО (гейт — не могу сейчас)
- **College Consulting** — новая роль + продукт (поступление/экзамены), нужен вход Эмира.
- **#2 миграции вместо db push** — нужен доступ к прод-Neon + осторожный baseline (риск на живой БД).
- **Сервер + hardening** — ждёт аренду сервера Intellect.
- **Полный текст трудового договора** — шаблон Эмира.
- **Индексация калькулятора** (noindex→open) — когда Эмир утвердит прайс v1.1.
- **Telegram webhook secret на проде** — ops (env), не код.

---

## В КОНЦЕ
- Все задачи закоммичены (по одной, отдельными коммитами) + задеплоены + проверены.
- **MD-сверка** `docs/СВЕРКА-прод-vs-реал.md`: что в проде (коммит+деплой) / что локально / что осталось — для сверки прод vs реал.
